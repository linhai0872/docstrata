# eval/.env — 复制为 .env 并填入真值，.env 已 gitignore 不会提交。
# 按使用场景选一种，只填对应的变量。

# ── 场景 A：聚合服务 ensemble（推荐）────────────────────────────────────────
# 一个 key 调多家模型，judges.yaml 默认即为三 judge ensemble（DeepSeek + GPT + Qwen）。
#
# ZenMux：https://zenmux.ai
# ZENMUX_API_KEY=
#
# OpenRouter：https://openrouter.ai
# OPENROUTER_API_KEY=

# ── 场景 B：多厂商直连 ensemble ──────────────────────────────────────────────
# 每家填自己的 key，在 judges.yaml 启用多个 judge（每个 judge 的 api_key_env 各指向一个 key）。
# 同时启用 ≥2 个 judge 时自动进入 ensemble 模式（取中位数+报分歧度）。
#
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# DEEPSEEK_API_KEY=

# ── 场景 C：单 judge 快速档 ────────────────────────────────────────────────
# 只启用一个 judge，填对应 key 即可。judges.yaml 里把其他 judge 的 enabled 改 false。
#
# OPENAI_API_KEY=

# ── 场景 D：任意端点（base_url + model + api_key 全在 .env 配）────────────────
# 配合 judges.yaml 里的 custom judge 范例使用，无需改 judges.yaml 其他内容。
#
# JUDGE_BASE_URL=https://api.openai.com/v1
# JUDGE_MODEL=gpt-4o
# JUDGE_API_KEY=

# ── 场景 E：本地 Ollama ────────────────────────────────────────────────────
# 无需 key。在 judges.yaml 启用 local judge 并修改 model 名，此文件留空。
