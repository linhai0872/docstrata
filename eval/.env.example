# 复制本文件为 .env 并填入真值。.env 已被 gitignore，不会提交。
# 只需填 judges.yaml 里 enabled judge 用到的那些 key——按需取舍，不必全填。
# 本地 Ollama 无需 key。

# 默认：ZenMux（OpenAI 兼容聚合，一个 key 调多家模型）
# 获取：https://zenmux.ai/platform/subscription (sk-ss-) 或 /pay-as-you-go (sk-ai-)
ZENMUX_API_KEY=

# 直连官方 / 其它聚合（在 judges.yaml 启用对应 judge 后填）
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENROUTER_API_KEY=
